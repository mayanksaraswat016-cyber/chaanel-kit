import { Channel } from '../channels/base';
import { Logger } from '../core/logger';
export declare class ApiServer {
    private port;
    private app;
    private httpServer;
    private server;
    private ctx;
    constructor(port?: number);
    setExposeDashboard(value: boolean): void;
    getExposeDashboard(): boolean;
    setApiSecret(secret: string | number | undefined): void;
    setMcpSecret(secret: string | number | undefined): void;
    setLogger(logger: Logger): void;
    setConfigPath(path: string): void;
    captureConsole(): void;
    registerChannel(name: string, channel: Channel): void;
    setPublicUrl(url: string): void;
    clearPublicUrl(): void;
    getBaseUrl(): string;
    getReplyUrl(channelName: string, jid: string): string;
    set findVoiceConfig(fn: ((channelName: string) => any) | undefined);
    set tunnelStart(fn: (() => Promise<{
        url: string;
    }>) | undefined);
    set tunnelStop(fn: (() => Promise<void>) | undefined);
    set tunnelStatus(fn: (() => {
        active: boolean;
        url: string | null;
    }) | undefined);
    set mcpStart(fn: (() => Promise<{
        url: string;
    }>) | undefined);
    set mcpStop(fn: (() => Promise<void>) | undefined);
    set mcpStatus(fn: (() => {
        active: boolean;
        url: string | null;
    }) | undefined);
    set updateStatus(fn: (() => Promise<any>) | undefined);
    set updateTrigger(fn: (() => Promise<any>) | undefined);
    set reloadRouter(fn: (() => void) | undefined);
    setExposeMcp(value: boolean): void;
    broadcast(msg: any): void;
    getExpressApp(): import("express-serve-static-core").Express;
    getPort(): number;
    start(): Promise<void>;
    private isPortInUse;
    /** Returns true if process was killed, a number for an alternative port, or false to abort. */
    private promptPortConflict;
    stop(): Promise<void>;
}
//# sourceMappingURL=server.d.ts.map