"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.installSkillCommand = installSkillCommand;
const path_1 = require("path");
const os_1 = require("os");
const fs_1 = require("fs");
const helpers_1 = require("../helpers");
const CLAUDE_MD_SNIPPET = `## ChannelKit

When working with WhatsApp, SMS, Voice, Telegram, or Email messaging:
- **ChannelKit has built-in TTS/STT.** Never install TTS or STT packages in the app. Configure TTS on the ChannelKit service — the app just returns \`{ "text": "..." }\` and ChannelKit converts it to audio automatically.
- **Use ChannelKit MCP tools** (get_status, send_message, add_channel, etc.) — not curl. If MCP tools are not available, ask the user to connect: \`claude mcp add --transport http channelkit http://localhost:4000/mcp\`
- **Do not install ChannelKit** unless the user explicitly asks — it's likely already running.
- Run \`/channelkit\` to load the full ChannelKit skill with detailed integration docs, MCP tool reference, and setup patterns.`;
function installSkillCommand(opts) {
    // From dist/cli/commands/ → go up 3 levels to package root
    const packageRoot = (0, path_1.dirname)((0, path_1.dirname)((0, path_1.dirname)(__dirname)));
    const skillSource = (0, path_1.join)(packageRoot, 'SKILL.md');
    if (!(0, fs_1.existsSync)(skillSource)) {
        console.error((0, helpers_1.c)('yellow', '\n  ❌ SKILL.md not found in ChannelKit package.\n'));
        process.exit(1);
    }
    // --print mode: output SKILL.md to stdout (for remote machines / piping)
    if (opts.print) {
        process.stdout.write((0, fs_1.readFileSync)(skillSource, 'utf-8'));
        return;
    }
    // Check if Claude Code is installed on this machine
    const claudeDir = (0, path_1.join)((0, os_1.homedir)(), '.claude');
    if (!(0, fs_1.existsSync)(claudeDir)) {
        console.log((0, helpers_1.c)('yellow', '\n  ⚠️  Claude Code does not appear to be installed on this machine.'));
        console.log((0, helpers_1.c)('dim', '  (~/.claude directory not found)\n'));
        console.log((0, helpers_1.c)('bright', '  If ChannelKit runs on a different machine than Claude Code,'));
        console.log((0, helpers_1.c)('bright', '  use --print to output the skill and copy it to your dev machine:\n'));
        console.log((0, helpers_1.c)('cyan', '  channelkit install-skill --print > channelkit-skill.md'));
        console.log((0, helpers_1.c)('dim', '  Then copy channelkit-skill.md to ~/.claude/skills/channelkit/SKILL.md on your dev machine.\n'));
        process.exit(1);
    }
    // Install the skill file
    const skillDir = (0, path_1.join)(claudeDir, 'skills', 'channelkit');
    const skillDest = (0, path_1.join)(skillDir, 'SKILL.md');
    (0, fs_1.mkdirSync)(skillDir, { recursive: true });
    (0, fs_1.copyFileSync)(skillSource, skillDest);
    console.log((0, helpers_1.c)('green', '\n  ✅ ChannelKit skill installed to ~/.claude/skills/channelkit/SKILL.md'));
    console.log((0, helpers_1.c)('dim', '  Use /channelkit in Claude Code to load the full integration guide.\n'));
    // Show CLAUDE.md snippet for the user to add manually
    const claudeMdPath = (0, path_1.join)(claudeDir, 'CLAUDE.md');
    const alreadyHas = (0, fs_1.existsSync)(claudeMdPath) && (0, fs_1.readFileSync)(claudeMdPath, 'utf-8').includes('## ChannelKit');
    if (!alreadyHas) {
        console.log((0, helpers_1.c)('bright', '  📋 For best results, add this to your ~/.claude/CLAUDE.md:\n'));
        console.log((0, helpers_1.c)('dim', '  ────────────────────────────────────────────'));
        for (const line of CLAUDE_MD_SNIPPET.split('\n')) {
            console.log((0, helpers_1.c)('dim', `  ${line}`));
        }
        console.log((0, helpers_1.c)('dim', '  ────────────────────────────────────────────\n'));
        console.log((0, helpers_1.c)('bright', '  This ensures Claude Code always knows about ChannelKit\'s built-in features.'));
        console.log((0, helpers_1.c)('dim', '  Without it, Claude may try to install TTS/STT packages in your app.\n'));
    }
}
//# sourceMappingURL=install-skill.js.map