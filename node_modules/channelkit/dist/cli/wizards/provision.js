"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.provisionWhatsAppWizard = provisionWhatsAppWizard;
const path_1 = require("path");
const fs_1 = require("fs");
const readline_1 = require("readline");
const helpers_1 = require("../helpers");
async function provisionWhatsAppWizard(opts) {
    (0, helpers_1.banner)();
    console.log((0, helpers_1.c)('bright', '  📱 Provision a WhatsApp number\n'));
    const rl = (0, readline_1.createInterface)({ input: process.stdin, output: process.stdout });
    try {
        console.log((0, helpers_1.c)('dim', '  We\'ll use Twilio to buy a number and receive the WhatsApp\n  verification SMS automatically.\n'));
        console.log((0, helpers_1.c)('dim', '  Don\'t have a Twilio account? Sign up at https://twilio.com\n'));
        const accountSid = await (0, helpers_1.ask)(rl, 'Twilio Account SID:');
        const authToken = await (0, helpers_1.ask)(rl, 'Twilio Auth Token:');
        if (!accountSid || !authToken) {
            console.log((0, helpers_1.c)('yellow', '\n  ❌ Account SID and Auth Token are required.\n'));
            rl.close();
            return;
        }
        console.log((0, helpers_1.c)('dim', '\n  Connecting to Twilio...'));
        const { TwilioProvisioner } = await Promise.resolve().then(() => __importStar(require('../../provisioning/twilio')));
        const twilio = new TwilioProvisioner({ accountSid, authToken });
        const country = await (0, helpers_1.ask)(rl, 'Country code (e.g. US, GB, IL):', 'US');
        console.log((0, helpers_1.c)('dim', `\n  Searching for mobile numbers in ${country}...\n`));
        let numbers;
        try {
            numbers = await twilio.searchNumbers(country, { type: 'mobile', limit: 5 });
        }
        catch (err) {
            console.log((0, helpers_1.c)('dim', '  No mobile numbers found, trying local numbers...\n'));
            numbers = await twilio.searchNumbers(country, { type: 'local', limit: 5 });
        }
        if (numbers.length === 0) {
            console.log((0, helpers_1.c)('yellow', '  ❌ No numbers available in this country.\n'));
            rl.close();
            return;
        }
        const idx = await (0, helpers_1.select)(rl, 'Choose a number:', numbers.map(n => {
            const features = [
                n.capabilities.sms ? 'SMS' : '',
                n.capabilities.voice ? 'Voice' : '',
            ].filter(Boolean).join(', ');
            return `${n.phoneNumber}  ${(0, helpers_1.c)('dim', `(${n.locality || n.region || n.isoCountry}) [${features}]`)}`;
        }));
        const chosen = numbers[idx];
        console.log((0, helpers_1.c)('dim', `\n  Purchasing ${chosen.phoneNumber}...`));
        const purchased = await twilio.purchaseNumber(chosen.phoneNumber);
        console.log((0, helpers_1.c)('green', `  ✅ Purchased: ${purchased.phoneNumber}\n`));
        const configPath = (0, path_1.resolve)(opts.config);
        if ((0, fs_1.existsSync)(configPath)) {
            const { loadConfig, saveConfig } = await Promise.resolve().then(() => __importStar(require('../../config/parser')));
            const config = loadConfig(configPath);
            const waKey = Object.keys(config.channels).find(k => config.channels[k].type === 'whatsapp') || 'whatsapp';
            config.channels[waKey] = { type: 'whatsapp', number: purchased.phoneNumber };
            config.providers = config.providers || {};
            config.providers.twilio = { accountSid, numberSid: purchased.sid };
            saveConfig(configPath, config);
            console.log((0, helpers_1.c)('dim', `  Updated ${configPath} with new number.\n`));
        }
        console.log((0, helpers_1.c)('bright', '\n  📲 Step 2: Register with WhatsApp\n'));
        console.log((0, helpers_1.c)('white', '  Now add this number as a second account on your WhatsApp:\n'));
        console.log((0, helpers_1.c)('dim', '  1. Open WhatsApp on your phone'));
        console.log((0, helpers_1.c)('dim', '  2. Go to Settings → Add Account (or switch accounts)'));
        console.log((0, helpers_1.c)('dim', `  3. Enter the new number: ${(0, helpers_1.c)('cyan', purchased.phoneNumber)}`));
        console.log((0, helpers_1.c)('dim', '  4. WhatsApp will send an SMS to verify the number'));
        console.log((0, helpers_1.c)('dim', '  5. We\'ll catch the SMS automatically via Twilio!\n'));
        const ready = await (0, helpers_1.ask)(rl, 'Ready? Press Enter when you\'ve requested the SMS verification in WhatsApp...');
        console.log((0, helpers_1.c)('dim', '\n  ⏳ Waiting for WhatsApp verification SMS...'));
        let dots = 0;
        const code = await twilio.waitForSms(purchased.phoneNumber, 120000, () => {
            dots = (dots + 1) % 4;
            process.stdout.write(`\r  ⏳ Waiting for SMS${'·'.repeat(dots)}${' '.repeat(3 - dots)}  `);
        });
        console.log(`\r                                        `);
        console.log((0, helpers_1.c)('green', `\n  ╔═══════════════════════════════════════╗`));
        console.log((0, helpers_1.c)('green', `  ║                                       ║`));
        console.log((0, helpers_1.c)('green', `  ║   📬 Verification code: ${(0, helpers_1.c)('bright', code)}        ║`));
        console.log((0, helpers_1.c)('green', `  ║                                       ║`));
        console.log((0, helpers_1.c)('green', `  ╚═══════════════════════════════════════╝`));
        console.log((0, helpers_1.c)('white', `\n  Enter this code in WhatsApp to complete registration.\n`));
        await (0, helpers_1.ask)(rl, 'Press Enter once WhatsApp is set up on the new number...');
        console.log((0, helpers_1.c)('bright', '\n  🔗 Step 3: Connect ChannelKit\n'));
        console.log((0, helpers_1.c)('dim', '  Now we\'ll connect ChannelKit as a linked device...\n'));
        let baileysModule;
        try {
            baileysModule = await Promise.resolve().then(() => __importStar(require('@whiskeysockets/baileys')));
        }
        catch {
            console.log((0, helpers_1.c)('yellow', '\n  ❌ @whiskeysockets/baileys is not installed.'));
            console.log((0, helpers_1.c)('white', '  Install it with: npm install @whiskeysockets/baileys\n'));
            rl.close();
            return;
        }
        const { useMultiFileAuthState, makeCacheableSignalKeyStore } = baileysModule;
        const makeWASocket = baileysModule.default;
        const { join } = await Promise.resolve().then(() => __importStar(require('path')));
        const { mkdirSync, existsSync: dirExists } = await Promise.resolve().then(() => __importStar(require('fs')));
        const { DEFAULT_AUTH_DIR } = await Promise.resolve().then(() => __importStar(require('../../paths')));
        const authDir = join(DEFAULT_AUTH_DIR, `whatsapp-provisioned`);
        if (!dirExists(authDir))
            mkdirSync(authDir, { recursive: true });
        const { state, saveCreds } = await useMultiFileAuthState(authDir);
        const phoneForPairing = purchased.phoneNumber.replace(/[^0-9]/g, '');
        const sock = makeWASocket({
            auth: {
                creds: state.creds,
                keys: makeCacheableSignalKeyStore(state.keys, undefined),
            },
        });
        sock.ev.on('creds.update', saveCreds);
        await new Promise((resolveConn, rejectConn) => {
            let pairingRequested = false;
            const timeout = setTimeout(() => {
                sock.end(undefined);
                rejectConn(new Error('Connection timeout'));
            }, 120000);
            sock.ev.on('connection.update', async (update) => {
                const { connection, lastDisconnect, qr } = update;
                if ((connection === 'connecting' || qr) && !pairingRequested) {
                    pairingRequested = true;
                    try {
                        const pairingCode = await sock.requestPairingCode(phoneForPairing);
                        const formatted = pairingCode.match(/.{1,4}/g)?.join('-') || pairingCode;
                        console.log((0, helpers_1.c)('green', '  ╔═══════════════════════════════════════╗'));
                        console.log((0, helpers_1.c)('green', '  ║                                       ║'));
                        console.log((0, helpers_1.c)('green', `  ║   Pairing code: ${(0, helpers_1.c)('bright', formatted)}          ║`));
                        console.log((0, helpers_1.c)('green', '  ║                                       ║'));
                        console.log((0, helpers_1.c)('green', '  ╚═══════════════════════════════════════╝'));
                        console.log();
                        console.log((0, helpers_1.c)('white', '  On your phone:'));
                        console.log((0, helpers_1.c)('dim', `  1. Switch to the ${purchased.phoneNumber} account in WhatsApp`));
                        console.log((0, helpers_1.c)('dim', '  2. Go to Settings → Linked Devices → Link a Device'));
                        console.log((0, helpers_1.c)('dim', '  3. Tap "Link with phone number instead"'));
                        console.log((0, helpers_1.c)('dim', `  4. Enter: ${purchased.phoneNumber}`));
                        console.log((0, helpers_1.c)('dim', `  5. Enter the pairing code: ${formatted}`));
                        console.log((0, helpers_1.c)('dim', '\n  Waiting for pairing...\n'));
                    }
                    catch (err) {
                        clearTimeout(timeout);
                        sock.end(undefined);
                        rejectConn(new Error(`Failed to request pairing code: ${err.message}`));
                    }
                }
                if (connection === 'open') {
                    clearTimeout(timeout);
                    sock.end(undefined);
                    resolveConn();
                }
                if (connection === 'close') {
                    const reason = lastDisconnect?.error?.output?.statusCode;
                    if (reason === 515) {
                        clearTimeout(timeout);
                        sock.end(undefined);
                        resolveConn();
                    }
                }
            });
        });
        console.log((0, helpers_1.c)('green', '  ╔═══════════════════════════════════════════╗'));
        console.log((0, helpers_1.c)('green', '  ║                                           ║'));
        console.log((0, helpers_1.c)('green', '  ║   🎉 Provisioning complete!               ║'));
        console.log((0, helpers_1.c)('green', '  ║                                           ║'));
        console.log((0, helpers_1.c)('green', `  ║   Number: ${purchased.phoneNumber.padEnd(28)}║`));
        console.log((0, helpers_1.c)('green', '  ║   WhatsApp: Connected ✅                  ║'));
        console.log((0, helpers_1.c)('green', '  ║                                           ║'));
        console.log((0, helpers_1.c)('green', '  ╚═══════════════════════════════════════════╝'));
        console.log();
        console.log((0, helpers_1.c)('white', '  Start ChannelKit:\n'));
        console.log((0, helpers_1.c)('cyan', '    npm start\n'));
    }
    catch (err) {
        console.error((0, helpers_1.c)('yellow', `\n  ❌ ${err.message}\n`));
    }
    finally {
        rl.close();
    }
}
//# sourceMappingURL=provision.js.map