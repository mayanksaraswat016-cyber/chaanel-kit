"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.channelAddWizard = channelAddWizard;
const path_1 = require("path");
const fs_1 = require("fs");
const readline_1 = require("readline");
const helpers_1 = require("../helpers");
const paths_1 = require("../../paths");
async function channelAddWizard(opts) {
    const configPath = (0, path_1.resolve)(opts.config);
    if (!(0, fs_1.existsSync)(configPath)) {
        console.error((0, helpers_1.c)('yellow', `\n  ❌ Config file not found: ${configPath}\n  Run 'channelkit init' first.\n`));
        process.exit(1);
    }
    const { loadConfig, saveConfig } = await Promise.resolve().then(() => __importStar(require('../../config/parser')));
    const config = loadConfig(configPath);
    const rl = (0, readline_1.createInterface)({ input: process.stdin, output: process.stdout });
    try {
        console.log((0, helpers_1.c)('cyan', '\n  📡 Add a new channel\n'));
        const channelIdx = await (0, helpers_1.select)(rl, 'Which channel do you want to add?', [
            '📱 WhatsApp — connect with your phone number',
            '💬 Telegram — create a bot',
            '📧 Email — Gmail or Resend',
            '📲 SMS — Twilio',
            '📞 Voice — Twilio',
        ]);
        let channelName = '';
        let channelConfig = {};
        if (channelIdx === 0) {
            const existingWa = Object.entries(config.channels).find(([, v]) => v.type === 'whatsapp');
            if (existingWa) {
                const overwrite = await (0, helpers_1.ask)(rl, `WhatsApp channel "${existingWa[0]}" already exists. Replace? (y/N):`, 'N');
                if (overwrite.toLowerCase() !== 'y') {
                    rl.close();
                    return;
                }
                delete config.channels[existingWa[0]];
                if (config.services) {
                    for (const [k, v] of Object.entries(config.services)) {
                        if (v.channel === existingWa[0])
                            delete config.services[k];
                    }
                }
                config.routes = (config.routes || []).filter(r => r.channel !== existingWa[0]);
            }
            console.log();
            const number = await (0, helpers_1.ask)(rl, 'Your WhatsApp phone number (with country code):', '+972...');
            const modeIdx = await (0, helpers_1.select)(rl, 'How will you use this channel?', [
                '👥 Groups mode — one number for multiple services',
                '📱 Service mode — one number for one service',
            ]);
            channelName = 'whatsapp';
            channelConfig = { type: 'whatsapp', number, mode: modeIdx === 0 ? 'groups' : 'direct' };
            const accessIdx = await (0, helpers_1.select)(rl, 'Who can send messages through this channel?', [
                '🌐 Any number — no restrictions',
                '🔒 Only specific numbers — allow list',
            ]);
            if (accessIdx === 1) {
                const numbers = await (0, helpers_1.ask)(rl, 'Allowed numbers (comma-separated, e.g. +972541234567, +12025551234):');
                if (numbers.trim()) {
                    channelConfig.allow_list = numbers.split(',').map((n) => n.trim()).filter(Boolean);
                }
            }
        }
        else if (channelIdx === 1) {
            const existingTg = Object.entries(config.channels).find(([, v]) => v.type === 'telegram');
            if (existingTg) {
                const overwrite = await (0, helpers_1.ask)(rl, `Telegram channel "${existingTg[0]}" already exists. Replace? (y/N):`, 'N');
                if (overwrite.toLowerCase() !== 'y') {
                    rl.close();
                    return;
                }
                delete config.channels[existingTg[0]];
                if (config.services) {
                    for (const [k, v] of Object.entries(config.services)) {
                        if (v.channel === existingTg[0])
                            delete config.services[k];
                    }
                }
                config.routes = (config.routes || []).filter(r => r.channel !== existingTg[0]);
            }
            console.log();
            console.log((0, helpers_1.c)('bright', '  📝 Quick setup — takes 30 seconds:\n'));
            console.log((0, helpers_1.c)('white', '  1.') + (0, helpers_1.c)('dim', ' Open Telegram and search for ') + (0, helpers_1.c)('cyan', '@BotFather'));
            console.log((0, helpers_1.c)('white', '  2.') + (0, helpers_1.c)('dim', ' Send ') + (0, helpers_1.c)('cyan', '/newbot'));
            console.log((0, helpers_1.c)('white', '  3.') + (0, helpers_1.c)('dim', ' Choose a name (e.g. "My Service Bot")'));
            console.log((0, helpers_1.c)('white', '  4.') + (0, helpers_1.c)('dim', ' Choose a username (e.g. "myservice_bot")'));
            console.log((0, helpers_1.c)('white', '  5.') + (0, helpers_1.c)('dim', ' Copy the token BotFather gives you\n'));
            const openLink = await (0, helpers_1.ask)(rl, 'Open BotFather in browser? (Y/n):', 'Y');
            if (openLink.toLowerCase() !== 'n') {
                const { exec: execCmd } = await Promise.resolve().then(() => __importStar(require('child_process')));
                execCmd('open "https://t.me/BotFather" 2>/dev/null || xdg-open "https://t.me/BotFather" 2>/dev/null');
            }
            console.log();
            const token = await (0, helpers_1.ask)(rl, 'Paste the bot token here:');
            if (!token || !token.includes(':')) {
                console.log((0, helpers_1.c)('yellow', '\n  ⚠️  That doesn\'t look like a valid token. It should look like: 123456:ABC-DEF...\n'));
                rl.close();
                return;
            }
            console.log((0, helpers_1.c)('dim', '\n  Verifying token...'));
            try {
                const res = await fetch(`https://api.telegram.org/bot${token}/getMe`);
                const data = await res.json();
                if (data.ok) {
                    console.log((0, helpers_1.c)('green', `  ✅ Connected to @${data.result.username} (${data.result.first_name})\n`));
                }
                else {
                    console.log((0, helpers_1.c)('yellow', `\n  ❌ Invalid token: ${data.description}\n`));
                    rl.close();
                    return;
                }
            }
            catch {
                console.log((0, helpers_1.c)('yellow', '\n  ⚠️  Could not verify token (no internet?). Continuing anyway.\n'));
            }
            channelName = 'telegram';
            channelConfig = { type: 'telegram', bot_token: token };
        }
        else if (channelIdx === 2) {
            console.log();
            const providerIdx = await (0, helpers_1.select)(rl, 'Which email provider?', [
                '📧 Gmail — OAuth2, polling (no public URL needed)',
                '📨 Resend — API + inbound webhooks (needs public URL)',
            ]);
            if (providerIdx === 0) {
                console.log((0, helpers_1.c)('bright', '\n  📝 Gmail OAuth Setup:\n'));
                console.log((0, helpers_1.c)('dim', '  You need a Google Cloud project with Gmail API enabled.'));
                console.log((0, helpers_1.c)('dim', '  Create OAuth2 credentials (Desktop app) at:'));
                console.log((0, helpers_1.c)('cyan', '  https://console.cloud.google.com/apis/credentials\n'));
                const clientId = await (0, helpers_1.ask)(rl, 'OAuth Client ID:');
                const clientSecret = await (0, helpers_1.ask)(rl, 'OAuth Client Secret:');
                const pollInterval = await (0, helpers_1.ask)(rl, 'Poll interval (seconds):', '30');
                channelName = await (0, helpers_1.ask)(rl, 'Channel name:', 'gmail');
                channelConfig = { type: 'email', provider: 'gmail', client_id: clientId, client_secret: clientSecret, poll_interval: parseInt(pollInterval) || 30 };
            }
            else {
                console.log((0, helpers_1.c)('bright', '\n  📝 Resend Setup:\n'));
                console.log((0, helpers_1.c)('dim', '  You need a Resend account with a verified domain.'));
                console.log((0, helpers_1.c)('dim', '  Get your API key at: https://resend.com/api-keys\n'));
                const apiKey = await (0, helpers_1.ask)(rl, 'Resend API Key:');
                const fromEmail = await (0, helpers_1.ask)(rl, 'Sender email (verified domain):', 'noreply@yourdomain.com');
                channelName = await (0, helpers_1.ask)(rl, 'Channel name:', 'resend');
                channelConfig = { type: 'email', provider: 'resend', api_key: apiKey, from_email: fromEmail };
                console.log((0, helpers_1.c)('dim', `\n  📬 Configure Resend inbound webhook to:`));
                console.log((0, helpers_1.c)('cyan', `     <your-public-url>/inbound/resend/${channelName}\n`));
            }
        }
        else if (channelIdx === 3) {
            console.log((0, helpers_1.c)('bright', '\n  📝 Twilio SMS Setup:\n'));
            console.log((0, helpers_1.c)('dim', '  You need a Twilio account with a phone number.'));
            console.log((0, helpers_1.c)('dim', '  Get credentials at: https://console.twilio.com\n'));
            const accountSid = await (0, helpers_1.ask)(rl, 'Account SID:');
            const authToken = await (0, helpers_1.ask)(rl, 'Auth Token:');
            const number = await (0, helpers_1.ask)(rl, 'Twilio phone number (e.g. +12025551234):');
            const pollInterval = await (0, helpers_1.ask)(rl, 'Poll interval in seconds (0 = webhook mode):', '10');
            channelName = await (0, helpers_1.ask)(rl, 'Channel name:', 'sms');
            channelConfig = {
                type: 'sms', provider: 'twilio', account_sid: accountSid, auth_token: authToken, number,
                ...(parseInt(pollInterval) > 0 ? { poll_interval: parseInt(pollInterval) } : {}),
            };
            const smsAccessIdx = await (0, helpers_1.select)(rl, 'Who can send messages through this channel?', [
                '🌐 Any number — no restrictions',
                '🔒 Only specific numbers — allow list',
            ]);
            if (smsAccessIdx === 1) {
                const smsNumbers = await (0, helpers_1.ask)(rl, 'Allowed numbers (comma-separated, e.g. +972541234567, +12025551234):');
                if (smsNumbers.trim()) {
                    channelConfig.allow_list = smsNumbers.split(',').map((n) => n.trim()).filter(Boolean);
                }
            }
        }
        else if (channelIdx === 4) {
            console.log((0, helpers_1.c)('bright', '\n  📝 Twilio Voice Setup:\n'));
            const existingSms = Object.entries(config.channels).find(([, v]) => v.type === 'sms' && v.provider === 'twilio');
            let accountSid;
            let authToken;
            if (existingSms) {
                const smsCfg = existingSms[1];
                const reuse = await (0, helpers_1.ask)(rl, `Reuse Twilio credentials from "${existingSms[0]}" channel? (Y/n):`, 'Y');
                if (reuse.toLowerCase() !== 'n') {
                    accountSid = smsCfg.account_sid;
                    authToken = smsCfg.auth_token;
                    console.log((0, helpers_1.c)('dim', '  Using existing Twilio credentials.\n'));
                }
                else {
                    accountSid = await (0, helpers_1.ask)(rl, 'Account SID:');
                    authToken = await (0, helpers_1.ask)(rl, 'Auth Token:');
                }
            }
            else {
                accountSid = await (0, helpers_1.ask)(rl, 'Account SID:');
                authToken = await (0, helpers_1.ask)(rl, 'Auth Token:');
            }
            const number = await (0, helpers_1.ask)(rl, 'Twilio phone number (e.g. +12025551234):');
            channelName = await (0, helpers_1.ask)(rl, 'Channel name:', 'voice');
            channelConfig = { type: 'voice', provider: 'twilio', account_sid: accountSid, auth_token: authToken, number };
            const voiceAccessIdx = await (0, helpers_1.select)(rl, 'Who can call this channel?', [
                '🌐 Any number — no restrictions',
                '🔒 Only specific numbers — allow list',
            ]);
            if (voiceAccessIdx === 1) {
                const voiceNumbers = await (0, helpers_1.ask)(rl, 'Allowed numbers (comma-separated, e.g. +972541234567, +12025551234):');
                if (voiceNumbers.trim()) {
                    channelConfig.allow_list = voiceNumbers.split(',').map((n) => n.trim()).filter(Boolean);
                }
            }
            console.log((0, helpers_1.c)('yellow', '\n  ⚠️  Voice requires a public URL. Use --tunnel or --public-url when starting.\n'));
        }
        config.channels[channelName] = channelConfig;
        saveConfig(configPath, config);
        console.log((0, helpers_1.c)('green', `\n  ✅ Channel "${channelName}" added!\n`));
        if (channelConfig.type === 'whatsapp') {
            console.log((0, helpers_1.c)('bright', '  Let\'s connect your WhatsApp now. Scan the QR code:\n'));
            console.log((0, helpers_1.c)('dim', '  Open WhatsApp → Settings → Linked Devices → Link a Device\n'));
            try {
                const { WhatsAppChannel } = await Promise.resolve().then(() => __importStar(require('../../channels/whatsapp')));
                const authPath = (0, path_1.join)(paths_1.DEFAULT_AUTH_DIR, `whatsapp-${channelName}`);
                await WhatsAppChannel.pair(authPath);
                console.log((0, helpers_1.c)('green', '\n  ✅ WhatsApp connected!\n'));
            }
            catch (err) {
                console.log((0, helpers_1.c)('yellow', `\n  ⚠️  Pairing failed: ${err.message}`));
                console.log((0, helpers_1.c)('dim', '  You can scan the QR code later when you start ChannelKit.\n'));
            }
        }
        else if (channelName === 'telegram') {
            console.log((0, helpers_1.c)('dim', '  Start ChannelKit to activate the Telegram bot.\n'));
        }
        const modeOptions = [
            '📦 Single service — one webhook for all messages',
            '📦 Multiple services — route to different webhooks (WhatsApp: magic codes, Telegram: slash commands)',
        ];
        const modeIdx = await (0, helpers_1.select)(rl, 'How will this channel be used?', modeOptions);
        if (modeIdx === 0) {
            const webhook = await (0, helpers_1.ask)(rl, 'Webhook URL:', 'http://localhost:3000');
            if (!config.services)
                config.services = {};
            const svcName = channelName;
            config.services[svcName] = { channel: channelName, webhook };
            saveConfig(configPath, config);
            console.log((0, helpers_1.c)('green', `\n  ✅ Service "${svcName}" created → ${webhook}\n`));
        }
        else {
            console.log((0, helpers_1.c)('dim', '\n  Run `channelkit service add` to add services to this channel.\n'));
        }
    }
    finally {
        rl.close();
    }
}
//# sourceMappingURL=channel.js.map