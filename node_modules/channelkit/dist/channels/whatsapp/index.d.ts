import { Channel } from '../base';
import { WhatsAppChannelConfig } from '../../config/types';
import { WebhookResponse } from '../../core/types';
export declare function isBaileysAvailable(): boolean;
export declare class WhatsAppChannel extends Channel {
    private processedIds;
    /**
     * Pair a new WhatsApp device by showing a QR code.
     * Creates a temporary Baileys connection, waits for successful pairing, then disconnects.
     * Auth state is persisted to authDir for later use by connect().
     */
    static pair(authDir: string, onQR?: (qr: string) => void): Promise<void>;
    private sock;
    private authDir;
    private reconnectAttempts;
    private reconnectTimer;
    private static readonly MAX_RECONNECT_ATTEMPTS;
    constructor(name: string, config: WhatsAppChannelConfig, authDir?: string);
    private scheduleReconnect;
    getSocket(): any;
    createGroup(name: string, participants: string[]): Promise<{
        id: string;
        subject: string;
    }>;
    sendToJid(jid: string, text: string): Promise<void>;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    send(to: string, response: WebhookResponse): Promise<void>;
    private toUnified;
}
//# sourceMappingURL=index.d.ts.map