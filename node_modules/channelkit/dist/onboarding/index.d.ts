import { OnboardingConfig } from '../config/types';
import { WhatsAppChannel } from '../channels/whatsapp';
import { TelegramChannel } from '../channels/telegram';
import { GroupStore } from '../core/groupStore';
import { UnifiedMessage } from '../core/types';
export declare class Onboarding {
    private config;
    private whatsappChannel?;
    private telegramChannel?;
    private groupStore;
    private telegramMappings;
    constructor(config: OnboardingConfig, whatsappChannel?: WhatsAppChannel | undefined, telegramChannel?: TelegramChannel | undefined);
    getGroupStore(): GroupStore;
    /**
     * Handle a direct message for onboarding. Returns true if handled.
     * @param unmatchedPolicy - what to do when no code matches ('list' = send menu, 'ignore' or undefined = silent)
     */
    handleDirectMessage(message: UnifiedMessage, unmatchedPolicy?: 'list' | 'ignore'): Promise<boolean>;
    /**
     * Check if a Telegram chat has a service mapping. Returns the webhook URL if so.
     */
    getTelegramServiceWebhook(chatId: string): string | undefined;
    private handleWhatsApp;
    private createWhatsAppGroup;
    private handleTelegram;
    private getCodesForChannel;
}
//# sourceMappingURL=index.d.ts.map