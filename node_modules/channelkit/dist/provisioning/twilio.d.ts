export interface TwilioConfig {
    accountSid: string;
    authToken: string;
}
export interface AvailableNumber {
    phoneNumber: string;
    friendlyName: string;
    locality?: string;
    region?: string;
    isoCountry: string;
    capabilities: {
        sms: boolean;
        mms: boolean;
        voice: boolean;
    };
    price?: string;
    priceUnit?: string;
}
export interface PurchasedNumber {
    sid: string;
    phoneNumber: string;
    friendlyName: string;
}
export declare class TwilioProvisioner {
    private client;
    constructor(config: TwilioConfig);
    listOwnedNumbers(): Promise<{
        phoneNumber: string;
        friendlyName: string;
        capabilities: {
            sms: boolean;
            mms: boolean;
            voice: boolean;
        };
    }[]>;
    searchNumbers(countryCode: string, options?: {
        type?: 'mobile' | 'local';
        limit?: number;
        smsEnabled?: boolean;
        voiceEnabled?: boolean;
    }): Promise<AvailableNumber[]>;
    purchaseNumber(phoneNumber: string): Promise<PurchasedNumber>;
    private findApprovedBundle;
    /**
     * Poll Twilio for incoming SMS to a specific number.
     * Returns the 6-digit verification code when found.
     */
    waitForSms(phoneNumber: string, timeoutMs?: number, onPoll?: () => void): Promise<string>;
    releaseNumber(numberSid: string): Promise<void>;
}
//# sourceMappingURL=twilio.d.ts.map