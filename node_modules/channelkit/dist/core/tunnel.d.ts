import { TunnelConfig } from '../config/types';
export declare class TunnelManager {
    private config;
    private targetPort;
    private publicUrl;
    private tunnel;
    constructor(config: TunnelConfig, targetPort: number);
    getPublicUrl(): string | null;
    start(): Promise<void>;
    stop(): Promise<void>;
    /** Named tunnel with a Cloudflare Tunnel token — gives a stable URL. */
    private startWithToken;
    /** Quick tunnel (trycloudflare.com) — random URL each time. */
    private startQuickTunnel;
}
//# sourceMappingURL=tunnel.d.ts.map