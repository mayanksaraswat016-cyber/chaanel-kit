import { ChannelConfig, SettingsConfig } from '../config/types';
interface ServiceInfo {
    name: string;
    webhook: string;
    description?: string;
}
interface AIRouterResult {
    serviceName: string | null;
    error?: string;
}
/**
 * Use an AI provider to classify which service a message should be routed to.
 */
export declare function aiRoute(message: string, services: ServiceInfo[], channelConfig: ChannelConfig, settings: SettingsConfig): Promise<AIRouterResult>;
export {};
//# sourceMappingURL=aiRouter.d.ts.map