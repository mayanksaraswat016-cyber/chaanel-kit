import { Channel } from '../channels/base';
export interface UpdateStatus {
    mode: 'git' | 'npm' | 'npx';
    currentVersion: string;
    latestVersion: string;
    updateAvailable: boolean;
    behindCount: number;
    lastChecked: number;
}
export interface UpdateResult {
    success: boolean;
    previousVersion: string;
    newVersion: string;
    error?: string;
}
export declare class Updater {
    private channels;
    private onLog?;
    private lastStatus;
    private autoUpdateTimer;
    private projectRoot;
    private updating;
    private mode;
    constructor(channels: Map<string, Channel>, onLog?: ((message: string) => void) | undefined);
    private detectMode;
    getMode(): string;
    private log;
    private exec;
    getCurrentVersion(): string;
    getLastStatus(): UpdateStatus | null;
    checkForUpdate(): Promise<UpdateStatus>;
    private checkGitUpdate;
    private checkNpmUpdate;
    performUpdate(): Promise<UpdateResult>;
    private performGitUpdate;
    private performNpmUpdate;
    private isGlobalInstall;
    /** Check for updates periodically without installing (notify only). */
    startUpdateCheck(intervalMinutes: number): void;
    private notifyUpdateCheck;
    startAutoUpdate(intervalMinutes: number): void;
    stopAutoUpdate(): void;
    private autoUpdateCheck;
}
//# sourceMappingURL=updater.d.ts.map